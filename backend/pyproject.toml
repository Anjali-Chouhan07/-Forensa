[project]
name = "forensa-backend"
version = "0.1.0"
dependencies = [
   "fastapi",
    "uvicorn",
    "pydantic",
    "sqlalchemy",
    "python-multipart",
    "web3",
    "opencv-python-headless"
]

[tool.uv]
managed = true